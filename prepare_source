git_src --branch debian/1.21.3-3 https://salsa.debian.org/debian/krb5

# Edit the control file so it will build against the 1443  
# build environment.
sed -i 's/dpkg-dev (>= 1.22.5)/dpkg-dev (>= 1.22.4)/' $dir/src/debian/control

version_suffix=gl0+bp1443
